# Configuration for CircleCI
# https://circleci.com/gh/tony19/logback-android
version: 2.1

executors:
  android:
    docker:
      - image: cimg/android:2024.01.1
    working_directory: ~/code
    environment:
      JVM_OPTS: -Xmx3200m

jobs:
  build:
    executor: android
    steps:
      - checkout
      - restore_cache:
          key: jars-{{ checksum "build.gradle" }}-{{ checksum  "logback-android/build.gradle" }}
      - run:
          name: Install Android SDK platform
          command: sdkmanager "platforms;android-35"
      - run:
          name: Assemble outputs
          command: ./gradlew assembleDebug assembleRelease --parallel
      - save_cache:
          paths:
            - ~/.gradle
          key: jars-{{ checksum "build.gradle" }}-{{ checksum  "logback-android/build.gradle" }}
      - persist_to_workspace:
          root: .
          paths:
            - .
      - store_artifacts:
          path: logback-android/build/outputs
          destination: outputs

  lint:
    executor: android
    steps:
      - attach_workspace:
          at: ~/code
      - run:
          name: Lint
          command: ./gradlew lint --parallel
      - store_artifacts:
          path: logback-android/build/reports
          destination: reports

  test:
    executor: android
    steps:
      - attach_workspace:
          at: ~/code
      # run the two flavors' suites in separate invocations: with the
      # configuration cache Gradle parallelizes independent tasks within a
      # project, and two concurrent test JVMs make the socket/SMTP tests
      # (random-port mock servers) flaky on small executors
      - run:
          name: Run Tests (jdk11)
          command: ./gradlew testJdk11DebugUnitTest
      - run:
          name: Run Tests (jdk8)
          command: ./gradlew testJdk8DebugUnitTest
      - store_artifacts:
          path: logback-android/build/reports
          destination: reports
      - store_test_results:
          path: logback-android/build/test-results

  test_app:
    executor: android
    steps:
      - attach_workspace:
          at: ~/code
      - run:
          name: Checkout app code
          command: git clone https://github.com/tony19-sandbox/logback-test-app.git
      # Use the jdk8 variant here: its minSdk 21 floor is closest to the old
      # single artifact's, so the existing test app can consume it unchanged
      - run:
          name: Copy logback-android to app libs
          command: mkdir -p logback-test-app/app/libs && cp logback-android/build/outputs/aar/logback-android-jdk8-release.aar logback-test-app/app/libs/logback-android-release.aar
      - run:
          name: Test app
          command: cd logback-test-app && ./gradlew test

workflows:
  version: 2
  build_and_test:
    jobs:
      - build
      - lint:
          requires:
            - build
      - test:
          requires:
            - build
      - test_app:
          requires:
            - build
